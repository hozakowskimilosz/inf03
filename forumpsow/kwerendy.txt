1)

INSERT INTO `odpowiedzi`(`id`, `Pytania_id`, `konta_id`, `odpowiedz`) VALUES (8, 1, 5,'Przyda Ci się także wysoki stół, na którym postawisz pieska.');

2)

SELECT nick, YEAR(CURDATE())-rejestracja AS 'latNaForum' FROM konta WHERE nick LIKE "Jacek" OR nick LIKE "Tomek" OR nick LIKE "Monika";

3)

SELECT konta.nick, konta.postow, pytania.pytanie FROM konta INNER JOIN pytania USING(id) WHERE pytania.id=1;

4)

SELECT odpowiedzi.id, odpowiedzi.odpowiedz, konta.nick FROM odpowiedzi INNER JOIN konta ON odpowiedzi.konta_id=konta.id;